// Array.isArray returns a boolean if it is an array or not

const arr = ["Pommes", "Peches", "Poires"];
const str = "Pommes";

let bool = Array.isArray(arr)

let checkStr = Array.isArray(str)

console.log(bool);
console.log(checkStr);