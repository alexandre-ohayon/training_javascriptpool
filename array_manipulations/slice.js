// slice returns selected elements of an array as a new array, from a given start to a not inclusive given end

const basket = ["fruits", "vegetables", "ice cream"];

const newBasket = basket.slice(2, 3);
console.log(newBasket);