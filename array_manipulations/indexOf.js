// indexOf gives the position of the string where there is the first occurence of the string, or return -1 if not found

const basket = ["Pommes", "Peches", "Poires"];

let idx = basket.indexOf("Pommes");

console.log(idx);